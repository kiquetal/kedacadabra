apiVersion: v1
kind: ServiceAccount
metadata:
  name: keda-cronjob-sa
  namespace: apps
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: keda-cronjob-role
  namespace: apps
rules:
- apiGroups: ["keda.sh"]
  resources: ["scaledobjects"]
  verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: keda-cronjob-rolebinding
  namespace: apps
subjects:
- kind: ServiceAccount
  name: keda-cronjob-sa
  namespace: apps
roleRef:
  kind: Role
  name: keda-cronjob-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: keda-pause-weekend
  namespace: apps
spec:
  schedule: "0 22 * * 5"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: keda-cronjob-sa
          restartPolicy: OnFailure
          containers:
          - name: kubectl
            image: registry.k8s.io/kubectl:v1.32.0
            command:
            - kubectl
            - annotate
            - scaledobject
            - demo-app-scaler
            - -n
            - apps
            - autoscaling.keda.sh/paused-replicas=0
            - --overwrite
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: keda-resume-monday
  namespace: apps
spec:
  schedule: "0 6 * * 1"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: keda-cronjob-sa
          restartPolicy: OnFailure
          containers:
          - name: kubectl
            image: registry.k8s.io/kubectl:v1.32.0
            command:
            - kubectl
            - annotate
            - scaledobject
            - demo-app-scaler
            - -n
            - apps
            - autoscaling.keda.sh/paused-replicas-
            - --overwrite
